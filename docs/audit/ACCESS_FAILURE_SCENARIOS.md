# MonClub Access Failure Scenario Matrix
# Round 6 — 2026-04-02

---

## Scenario matrix

| # | Scenario | Impacted Component | Expected Behavior | Actual Behavior | Risk | Current Handling | Sufficient? | Fix / Mitigation |
|---|----------|-------------------|-------------------|-----------------|------|-----------------|-------------|-----------------|
| 1 | Backend unavailable during sync | Sync timer, all modes | Serve from local cache; surface stale condition | Exception logged; cached data continues to be used; no staleness threshold | Medium | `except Exception: log + _last_sync_ok = False` | Partial — no max-age denial | Add `max_cache_age_hours` setting; log WARNING or deny after threshold |
| 2 | Backend returns empty user list | `save_sync_cache()` | Reject if likely error; keep previous cache | H-006 guard: rejects wipe if `old_count > 10` and `new_count == 0` | High | Guard in `db.py:1182-1191` | Yes — anti-wipe guard in place | Monitor for edge case: exactly 1–10 real users going to 0 |
| 3 | ZKTeco device offline (DEVICE mode sync) | `DeviceSyncEngine` | Skip device; retry next interval; saved hash stays dirty | `PullSDK.connect()` raises; loop catches; `save_device_sync_state(ok=False, hash=None)` | Low | Per-device try/except in `_sync_one_device` | Yes | None |
| 4 | ZKTeco device offline (AGENT mode polling) | `DeviceWorker` | Reconnect with exponential backoff | Backoff: 0.25s→30s; `_reconnects` counter incremented | Low | `time.sleep(backoff)` in DeviceWorker.run() | Yes | None |
| 5 | ZKTeco device offline (ULTRA mode polling) | `UltraDeviceWorker` | Reconnect; retry 5s | `self._stop.wait(5.0)` on connect failure | Low | Connect loop in `run()` | Yes | None |
| 6 | PullSDK watchdog timeout (AGENT) | `DeviceWorker` | Detect hang; force disconnect; reconnect | 15s watchdog thread; timeout → `disconnect()` + RuntimeError → reconnect | Low | F-021 in DeviceWorker.run() | Yes | None |
| 7 | PullSDK watchdog timeout (ULTRA) | `UltraDeviceWorker` | Detect hang; force reconnect | `_poll_with_watchdog()` — t.join(timeout=poll_timeout_sec); if alive → return None → disconnect | Low | `_poll_with_watchdog()` | Yes | None |
| 8 | SQLite DB error during history insert (AGENT) | `DecisionService` | Fail closed — do NOT open door | `_history_claimed = 0` on exception; door only opens if claimed | **Critical** | F-001 fix: `_history_claimed = 0` on except | Yes — fail-closed confirmed | Regression test covers this |
| 9 | SQLite DB error during history insert (ULTRA) | `UltraDeviceWorker` | Fail closed — do NOT open door | `inserted = False` on exception; door only opens if inserted=True | **Critical** | B-001 fix: `inserted = False` on except | Yes — fail-closed confirmed | Regression test covers this |
| 10 | Duplicate RTLog event (same event_id) | AGENT DecisionService, ULTRA UltraDeviceWorker | Process only once; no duplicate door-open | UNIQUE(event_id) + INSERT OR IGNORE; second call returns rowcount=0 | High | Atomic DB dedup gate | Yes | Pre-populated seen deque prevents replay after restart |
| 11 | RTLog event arrives after restart (old cursor) | AGENT DeviceWorker | Skip events before saved cursor | `_is_old_by_cursor()` + pre-populated seen deque from last 200 DB rows | Medium | F-016 pre-populate | Yes | Clock drift on device can defeat epoch comparison |
| 12 | Access history upload to backend fails | `DeviceAttendanceMaintenanceEngine` | Retry after 300s; row stays PENDING | `mark_access_history_sync_failure(retry_after_seconds=300)` | Medium | Fixed retry at 300s, no backoff | Partial — no jitter, no backoff | Add exponential backoff with jitter (post-launch) |
| 13 | Access history upload partial (some rows fail) | `DeviceAttendanceMaintenanceEngine` | Mark only failed rows for retry | `_parse_upload_response()` splits success/retryable/terminal IDs | Medium | Split success/retry parsing | Yes | Terminal failures (permanent) are flagged separately |
| 14 | Backend outage → multiple sites retry simultaneously | All sites | Stagger retries to avoid thundering herd | All sites use same 300s fixed delay | Medium | No jitter in retry delay | No | Add `random.uniform(0.5, 1.5) * 300s` jitter to retry delay |
| 15 | ULTRA devices double-synced (DEVICE+ULTRA mix) | `DeviceSyncEngine` + `UltraSyncScheduler` | Each handles its own mode; no overlap | DeviceSyncEngine processes ULTRA as DEVICE (M-NEW-001) | Medium | No guard — BUG | No | Add "ULTRA" to skip list in `_normalize_device()` (FIXED Round 6) |
| 16 | New ULTRA device ignored at runtime | `UltraEngine.start()` | Add device to running engine | `start()` returns early if engine running — new device ignored | Medium | No incremental add — BUG | No | Compare device list; restart engine if changed (FIXED Round 6) |
| 17 | DPAPI unavailable (Windows user profile issues) | `shared/auth_state.py` | Refuse to store token plaintext; log error | `protect_auth_token()` returns "" on failure; token not stored | High | B-002 fix: returns "" on DPAPI failure | Yes — fail-closed | User must re-login; no plaintext fallback |
| 18 | ZKFinger fingerprint HW error (AGENT mode) | `app/sdk/zkfinger.py` | Fail closed — return NO MATCH | OSError → `db_match()` returns 0 (no match) | High | H-005 fix: fail-closed on OSError | Yes | Fingerprint error logged; RFID path still available |
| 19 | Sync cache cleared / corrupted JSON | `db.py:load_sync_cache()` | Return None; engines skip safely | Malformed payload_json: exception → returns None; caller skips sync | Medium | try/except in load_sync_cache | Yes | Loss of local state; users locked out until re-sync from backend |
| 20 | SQLite file locked (WAL mode) | All DB callers | Wait up to 30s; raise OperationalError | `sqlite3.connect(..., timeout=30)` | Low | timeout=30 | Adequate | WAL reduces contention; multiple threads share same connection pool |
| 21 | Door command fails after valid TOTP (ULTRA) | `UltraDeviceWorker._open_door_with_retry()` | Retry once; mark as DOOR_CMD_FAILED | One retry with 100ms delay; `allowed=False` + reason="DOOR_CMD_FAILED" | Medium | Two-attempt retry | Adequate | User-visible message: "Valid code but door did not open" |
| 22 | Door command fails after valid TOTP (AGENT) | `DecisionService` → `DeviceCommandBus.open_door()` | Execute command; log result; notify | `cmd_res.ok = False` → notification includes cmdError | Medium | Command result logged | Adequate | No retry in AGENT mode — could add one retry |
| 23 | sync_tick fired while previous run is still active | `MainApp.sync_tick()` | Skip duplicate run | `_sync_work_running` guard — but flag set INSIDE thread (race window) | Low | Guard present but racy | Partial | Set flag before thread start (FIXED Round 6) |
| 24 | Shutdown during active sync | `MainApp.quit_app()` | Clean stop; engines drain | 15s force-exit guard + `_ultra_lock` guarded stop | Low | H-003 + H-004 fixes | Adequate | Force-exit ensures process terminates even if engine hangs |
| 25 | TOTP code collision (two credentials match) | `access_verification.verify_totp()` | Deny; log DENY_COLLISION | Returns `allowed=False, reason="DENY_COLLISION"` | High | Collision detection in verify_totp | Yes | Very unlikely with proper credential management |
| 26 | TOTP code age > max_past_age_seconds | `access_verification.verify_totp()` | Deny; log DENY_EXPIRED | Age checked: `if age > max_past_age: deny` | Medium | Age check | Yes | Default max_past_age = 32s (slightly more than one 30s period) |
| 27 | Card number matches TOTP format (false positive) | AGENT DecisionService / ULTRA `_is_totp_format()` | Attempt TOTP; fall back to RFID | TOTP fails → fallback: `verify_card(scanned=raw)` | Low | Fallback in verify_totp | Yes | Harmless; logged as CARD_FALLBACK_AFTER_TOTP_FAIL |
| 28 | Backend sync payload missing devices key | `db.py:save_sync_cache()` | Preserve previous device list | Empty devices → normalized table not updated if previous had devices | Medium | No specific guard for zero-devices | Partial | Add similar anti-wipe guard for devices as for users |
| 29 | Offline creation queue grows indefinitely | `access/store.py` | Prune old entries | `prune_offline_creation_queue()` called every 200 history writes | Medium | Pruning in HistoryService.run() | Adequate | Prune retention 30 days |
| 30 | TV component crash | `tv/` | No impact on access control | TV is a separate sub-service; access engines are in main app | Low | Component isolation | Yes | Access remains fully functional if TV runtime fails |
