# MonClub Access Findings Register
# Rounds 1–6 — cumulative (2026-04-02)

---

## Summary by severity

| Severity | Count | Fixed | Open |
|----------|-------|-------|------|
| Blocker  | 2     | 2     | 0    |
| High     | 5     | 5     | 0    |
| Medium   | 8     | 3     | 5    |
| Low      | 6     | 2     | 4    |

---

## Full register

| ID | Severity | Title | Affected Files / Functions | What Is Wrong | Why It Matters | Recommended Fix | Fixed? |
|---|---|---|---|---|---|---|---|
| B-001 | Blocker | Local API middleware never validated caller token | `app/api/local_access_api_v2.py:4227-4245` | Middleware only checked that a stored backend auth token existed; caller's X-Local-Token header was never validated. | Any local process could call door-open / user-push endpoints once the app was logged in. | Generate `secrets.token_urlsafe(32)` at startup; pass via env; validate on every non-exempt request. | **FIXED** Round 5 |
| B-002 | Blocker | Local API bind host could become 0.0.0.0 | `app/ui/app.py:659-670` | `_effective_local_api_bind()` accepted backend host values including 0.0.0.0. | Access-control API could become remotely reachable when combined with B-001. | Hardcode `host = "127.0.0.1"` unconditionally. | **FIXED** Round 5 |
| H-001 | High | Client/contract/server auth model not aligned | `tauri-ui/src/api/client.ts`, `tauri-ui/src-tauri/src/lib.rs`, `tauri-ui/src/runtime/desktopContext.ts` | Client sent X-Local-Token but `setAuthToken()` was never called; Rust context didn't expose token; server didn't validate. | Entire per-session token system was plumbed but effectively disabled. | Bootstrap: Python→env var→Rust→TypeScript→setAuthToken()→header. | **FIXED** Round 5 |
| H-002 | High | AGENT mode started HistoryService (dead idle thread) | `app/core/realtime_agent.py` | HistoryService was started in AGENT mode even though DecisionService writes history directly. | Idle thread; architectural confusion; double-write risk. | Do not start HistoryService in AGENT mode. | **FIXED** Round 4 |
| H-003 | High | No force-exit timeout on shutdown | `app/ui/app.py:559-588` | quit_app() had no deadline; a stuck engine could hang the process forever. | Operator could not restart the app without task manager. | 15s daemon thread force-exit via `os._exit(1)`. | **FIXED** Round 4 |
| H-004 | High | ULTRA start/stop not guarded by lock | `app/ui/app.py` | Concurrent sync_tick could start or stop UltraEngine in parallel. | Race: two start() calls, or start+stop overlap. | `_ultra_lock = threading.Lock()` guards all start/stop calls. | **FIXED** Round 4 |
| H-005 | High | /api/v2/status reported placeholder sync state | `app/api/local_access_api_v2.py:411-455` | Sync timestamps and ok/error fields were hardcoded placeholders. | Operators could not detect sync failures from the status endpoint. | Track `_last_sync_at/ok/error` in sync_tick and read in status handler. | **FIXED** Round 5 |
| M-001 | Medium | Queue sizes hardcoded in UltraEngine | `app/core/ultra_engine.py:778-782` | popup_q and history_q sizes hardcoded to 5000. | Could not be tuned from backend settings like AGENT mode. | Read from `get_backend_global_settings()` with 5000 fallback. | **FIXED** Round 4 |
| M-002 | Medium | RTLog poll timeout hardcoded in ULTRA | `app/core/ultra_engine.py:63` | `_poll_timeout_sec` was hardcoded 15s. | Cannot tune for slow networks or high-latency devices. | Read from `settings.get("rtlog_poll_timeout_sec", 15.0)`. | **FIXED** Round 4 |
| M-003 | Medium | No rate limit on door-open API endpoint | `app/api/local_access_api_v2.py:2642-2655` | No cooldown on `/api/v2/devices/{id}/door/open`. | Rapid repeated calls could spam PullSDK. | `_door_open_last` dict with 1s per-device cooldown; 429 response. | **FIXED** Round 4 |
| M-004 | Medium | ULTRA sync hash missed card and fingerprint changes | `app/core/ultra_engine.py:719-731` | Hash only included `activeMembershipId`; card or FP changes were invisible. | ULTRA device could serve stale card data until full resync. | Include `firstCardId`, `secondCardId`, `fingerprintsHash` in hash. | **FIXED** Round 4 |
| **M-NEW-001** | **Medium** | **ULTRA devices double-synced by DeviceSyncEngine** | `app/core/device_sync.py:206-210`, `app/ui/app.py:971` | `_normalize_device()` maps "ULTRA"→"DEVICE"; ULTRA devices processed by DeviceSyncEngine when DEVICE devices co-exist. | Concurrent PullSDK connections to the same device; potential sync failures; unnecessary load. | Add "ULTRA" to recognized set in `_normalize_device()`; skip ULTRA in `_sync_all_devices()`. | **FIXED** Round 6 |
| **M-NEW-002** | **Medium** | **New ULTRA device silently ignored by running engine** | `app/core/ultra_engine.py:798-804`, `app/ui/app.py:1010-1021` | `UltraEngine.start()` returns early if `self._running`. New ULTRA devices added while engine is running are never picked up. | Mixed-mode deployments: switching a device to ULTRA at runtime has no effect until app restart. | Compare current ULTRA device list against running workers; restart engine if list changed. | **FIXED** Round 6 |
| **M-NEW-003** | **Medium** | **`_sync_work_running` race window** | `app/ui/app.py:941-1058` | Flag set inside thread body, not before thread start. A second `_sync_tick` can pass the guard before the flag is set. | Two concurrent sync threads under unusual timer conditions. | Set flag before `threading.Thread(...).start()`. | **FIXED** Round 6 |
| M-005 | Medium | Fixed retry delay for history upload — no backoff/jitter | `app/core/device_attendance.py:28-29` | `UPLOAD_FAILURE_RETRY_SECONDS = 300` with no jitter. | Synchronized retry storm across sites on backend recovery. | Add jitter (e.g., `random.uniform(0.5, 1.5) * delay`) or exponential backoff. | Not fixed — post-launch |
| M-006 | Medium | ULTRA HistoryService redundant and has edge-case history_source risk | `app/core/ultra_engine.py`, `app/core/realtime_agent.py:1251-1307` | ULTRA worker inserts with history_source="ULTRA" before enqueue. HistoryService's second insert is OR IGNORE (no-op). If first insert fails, HistoryService would record history_source=NULL (→"AGENT"). | Misclassified history in rare failure case; architectural confusion. | Have ULTRA worker rely solely on its direct insert; use HistoryService only for pruning. Or add history_source to HistoryRecord dataclass and pass it through. | Not fixed — post-launch |
| L-001 | Low | `unprotect_auth_token` still handles "raw:" prefix | `shared/auth_state.py:75-77` | Legacy plaintext prefix handling remains. Existing installations with "raw:" tokens from before DPAPI migration continue to work. | DPAPI improvement not retroactively applied to existing tokens. | Run a one-time migration at startup: if token starts with "raw:", re-protect with DPAPI. | Not fixed — low priority |
| L-002 | Low | `pushingToDevicePolicy` read but never enforced | `app/core/device_sync.py:216-232`, `app/core/settings_reader.py:307` | Field is parsed and stored in device settings dict but no push logic consults it. | If the policy is supposed to constrain what data is pushed, it silently has no effect. | Either implement the policy or remove it from the settings model to avoid confusion. | Not fixed |
| L-003 | Low | `gym_access_secrets_cache.json` at project root | Root directory | File name implies it could contain real credentials. In a packaged build it should not be present. | Potential credential leak in build artifacts. | Verify file is gitignored and excluded from installer; remove if it contains real data. | Not verified |
| L-004 | Low | UltraSyncScheduler uses shortest interval across all devices | `app/core/ultra_engine.py:673-678` | `min_interval = min(all device sync intervals)`. One device with short interval forces all to sync at that rate. | Unnecessary device connections under mixed-interval deployments. | Independent per-device timers or at minimum log a warning when intervals differ. | Not fixed |
| L-005 | Low | Event queue put() in DeviceWorker has no drop path | `app/core/realtime_agent.py` (DeviceWorker.run()) | `event_queue.put(AccessEvent)` with no timeout — blocks when queue is full (maxsize=5000). | Under sustained burst, DeviceWorker polling thread could block indefinitely. | Use `put_nowait()` with drop logging, incrementing `_dropped_events`. | Not fixed |
| L-006 | Low | No stale-cache age warning or max-age policy | `app/core/realtime_agent.py`, `app/core/ultra_engine.py` | Neither AGENT nor ULTRA has a max-age for the local SQLite credential cache. An offline system continues to authorize indefinitely from stale data. | Operators cannot detect or be warned when local cache is dangerously old. | Add configurable `max_cache_age_hours`; log WARNING or deny if exceeded. | Not fixed — post-launch |
